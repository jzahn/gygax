JWT Key is duplicated in the docker compose file and in the .env. Shouldn't the docker compose file pull from the .env?
-or-
[X] None of the variables defined in the .env (example) file are being used by the docker compose file. They are hard coded. Is the intent that there will eventually be a docker-compose-prod.yml that will reference them and the current docker-compose is simply a dev version?

I am not crazy about the parchment background. I think I would prefer a plain whitebackground remenicent looking at the inside of the Moldavy Books. 
-or-
[X] When i am using a large screen the parchment background is maginified and does not look well. It seems tied to the width of the browser. I like how it looks on a small screen, but on a large screen the grain is large and yields what looks almost like chromatic abberations in the grain (unpleasent color splotches).

[X] Hovering over clickable links and buttons should cause my cursor to change. Currently it remains as the standard arrow regardless of what I am hovering over.

[X] We need to add a verify email feature in an upcoming spec (sooner rather than later). Right now anyone can sign into the site with a fake email address.